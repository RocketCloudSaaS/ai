AI Connections
==============

This module adds a generic ``api_request`` AI tool and configurable API
connections for HTTP integrations.

Usage
=====

- Go to ``AI > API Connections`` to define reusable external API connections.
- Use the ``api_request`` generic tool with either a direct ``url`` or a
  ``connection_name`` and relative ``endpoint``.
