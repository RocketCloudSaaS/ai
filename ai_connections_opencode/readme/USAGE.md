- Install this module after configuring the OpenCode bridge token in the
  deployment environment.
- Go to `AI > API Connections` to review the created OpenCode connection.
- Use the `opencode-connector` connection from AI tools that need to talk to
  OpenCode.

## Security
The OpenCode bridge token is stored as the connection authentication value.
Review access to API connections and limit it to system administrators.
